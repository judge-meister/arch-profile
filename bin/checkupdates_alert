#!/bin/bash
update_count=$(checkupdates | wc -l)
if [ "$update_count" -gt 0 ]
then
  notify-send "Pacman Updates" "$(checkupdates|cut -d' ' -f1,2)"\
    --icon=/usr/share/icons/Adwaita/512x512/legacy/emblem-package.png
fi

